package uniqueletterwords

/**
 * Given the list of words, find all sets of 5 5-letter-words using only unique letters. The alphabet has 26 letters, so 5 5-letter-words can take up to 25 unique letters.
 *
 * Example: fjord, gucks, nymph, vibex, waltz (letter 'q' is left over)
 */
fun findSetsOfFiveWordsWithUniqueLetters(words: List<String>): Set<Set<String>> {
    return emptySet()
}

