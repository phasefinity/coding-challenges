package oddint;

public class FindOddInt {

    /**
     * Finds the integer that has an odd (uneven) number of occurrences in an array of integers.
     */
    public static int findOddInt(int[] intArray) {
        return 0;
    }
}
